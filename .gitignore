.DS_*
*.log
logs
dist
coverage
**/*.backup.*
**/*.back.*

node_modules
bower_components

*.sublime*

.npmrc
psd
thumb
sketch
.atl/

# SDD working tree. Tooling artifacts only; never shipped (not in `files`
# in package.json) and not part of the published source. Listed here so a
# careless `git add .` cannot drag the whole changes/specs tree into a
# release commit.
openspec/
